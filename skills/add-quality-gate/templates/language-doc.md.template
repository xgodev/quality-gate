# Quality Gate -- {{LANGUAGE_NAME}}

{{LANGUAGE_NAME}}-specific gate documentation. For the common contract, see [`../contract.md`](../contract.md).

## Build system

Quality Gate {{LANGUAGE_NAME}} assumes **{{BUILD_SYSTEM}}** ({{BUILD_SYSTEM_RATIONALE}}).

<!--
TODO(template): explain the choice. If the language has several build systems
(pip vs poetry vs uv; npm vs pnpm vs bun; mvn vs gradle), say which one was chosen
and why. V1 does NOT support several.
-->

## Prerequisites with install

### macOS

```bash
{{INSTALL_MACOS}}
```

### Linux (Ubuntu/Debian)

```bash
{{INSTALL_LINUX}}
```

<!--
TODO(template): verifiable commands (run them in the shell before committing).
Example (Rust macOS):
  curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
  cargo install cargo-llvm-cov
  brew install jq
-->

## Metrics -- what each one measures in {{LANGUAGE_NAME}}

{{METRIC_DETAILS}}

<!--
TODO(template): a block PER MEASURED METRIC, not a generic copy.
For EACH metric use this format:

### `<name>` -- <short description>

Runs `<exact command>`. Counts `<what it counts>`.

**Configuration:** <reference to the config file if any, or "tool defaults">

**How to interpret a regression:** <explanation + suggested fix command>

Example (Rust, fmt):

### `fmt` -- formatting
Runs `cargo fmt --all -- --check`. Counts lines starting with `Diff in `.
**Configuration:** if the project has a `rustfmt.toml`, it is respected.
**How to interpret a regression:** the PR introduced an unformatted file. Fix: `cargo fmt --all`.
-->

## Common troubleshooting

{{TROUBLESHOOTING}}

<!--
TODO(template): mandatory to list THE 3 MOST LIKELY ERRORS + a fix for each.
Typical errors to consider:
- Stale baseline cache (`--refresh-baseline` or `rm -rf /tmp/qg-baseline-*`).
- `git archive` failing ("not a valid object name") -> `git fetch origin`.
- A coverage tool very slow or hanging -> a specific known-good version.
- (if applicable) auth missing -> instruction to set the env var correctly.
- (if applicable) different tooling between macOS BSD and Linux GNU.
-->

## Omitted metrics

{{OMITTED_METRICS}}

<!--
TODO(template): if you omitted one of the 6 reserved metrics (fmt, lint, build, test,
complexity, coverage), list it HERE with a per-language justification. Example:

- `coverage`: SQL DDL has no concept of executed-line coverage.
- `complexity`: bash has no canonical cyclomatic-complexity tool.

If you omitted nothing, write: "None. {{LANGUAGE_NAME}} supports the 6 reserved
metrics with official tools."

REMINDER: an omitted metric does NOT appear in the JSON nor in the text table. A 0
or null sentinel is FORBIDDEN.
-->

## Extra metrics

{{EXTRA_METRICS}}

<!--
TODO(template): if you added a metric beyond the 6 reserved, document:
- Name (snake_case ASCII, not colliding with reserved names).
- The tool used.
- The semantics (what exactly it counts).
- How to interpret a regression.
- How the user fixes it.

If you added nothing, write: "None in V1. Future candidates:" and list them.

Example (Rust):
- `audit` via `cargo audit` -- vulnerabilities in dependencies.
- `unsafe_count` via `cargo geiger` -- use of `unsafe` blocks.
-->
